#
# This is the Ant build.properties file for the SQLBroker project
#
app.name=sqlbroker
version=1.0

src=${basedir}/src
lib=${basedir}/lib
build=${basedir}/build

classdir=${build}/classes
distdir=${build}/dist

jarFile=${app.name}-${version}.jar
srcFile=${app.name}-${version}-src.zip
